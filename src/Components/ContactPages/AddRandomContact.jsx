import React from "react";

const AddRandomContacts = () => {
  return(
    <div>
      <button className='btn btn-danger form-control'>Add Random Contacts</button>
    </div>
  );
};

export default AddRandomContacts;